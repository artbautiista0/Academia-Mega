// Exportamos una función para saludar
export function saludar(nombre) {
    return `¡Hola, ${nombre}!`;
  }
  
  // Exportamos otra función para despedir
  export function despedir(nombre) {
    return `Adiós, ${nombre}!`;
  }
  